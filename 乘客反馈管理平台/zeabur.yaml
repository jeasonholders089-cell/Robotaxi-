# Zeabur Service Configuration - Frontend
kind: Service
spec:
  name: robotaxi-frontend-nginx
  runtime: docker
  port: 8080
  build:
    context: ./client
    dockerfile: ./Dockerfile
  env:
    - key: VITE_API_BASE_URL
      value: "/api"
