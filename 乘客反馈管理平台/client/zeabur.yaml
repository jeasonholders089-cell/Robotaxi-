# Zeabur Service Configuration - Frontend
kind: Service
spec:
  name: robotaxi-frontend
  runtime: docker
  port: 8080
  rootDirectory: /
  dockerfilePath: client/Dockerfile
  env:
    - key: VITE_API_BASE_URL
      value: "/api"
