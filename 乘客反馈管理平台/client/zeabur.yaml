# Zeabur Service Configuration - Frontend
kind: Service
spec:
  name: robotaxi-frontend
  runtime: docker
  port: 8080
  rootDirectory: 乘客反馈管理平台/client
  env:
    - key: VITE_API_BASE_URL
      value: "/api"
