build:
  language: node
  node:
    version: "20"
  installCommand: pnpm install --frozen-lockfile --ignore-scripts
  buildCommand: pnpm run build
  outputDirectory: dist
  port: 3000
  startCommand: pnpm run preview --port 3000 --host
